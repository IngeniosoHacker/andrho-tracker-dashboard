{
  "name": "webtracker-dashboard",
  "version": "1.0.0",
  "description": "AndRho landing (web/) + read-only analytics dashboard (public/dashboard/) for analytics-tracker. Dashboard access is JWT-based (issued by andrho-api), one site_id per account.",
  "main": "src/server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0"
  }
}
